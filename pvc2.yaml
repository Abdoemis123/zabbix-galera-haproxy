apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-my-release-mariadb-galera-1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: managed-premium
